import 'package:flutter/material.dart';

class TradingPartners extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Trading Partners'),
    );
  }
}
