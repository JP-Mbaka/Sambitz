import 'package:flutter/material.dart';

class Suspended extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Auspeneded'),
    );
  }
}
