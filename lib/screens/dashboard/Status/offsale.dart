import 'package:flutter/material.dart';

class OffSale extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Offsale'),
    );
  }
}
